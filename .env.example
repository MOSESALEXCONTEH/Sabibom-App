# Names only — never commit real values.
# Configure Firebase Functions secrets instead of client .env files:
#   firebase functions:secrets:set GROQ_API_KEY
#   firebase functions:secrets:set GROQ_MODEL
#   firebase functions:secrets:set PINATA_JWT
#   firebase functions:secrets:set PINATA_GATEWAY
#
# GROQ_API_KEY=
# GROQ_MODEL=
# PINATA_JWT=
# PINATA_GATEWAY=
