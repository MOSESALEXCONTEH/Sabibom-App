/// User-facing product copy shared across features.
abstract final class AppStrings {
  /// Product name.
  static const appName = 'SabiBom';

  /// Product promise shown on the splash screen.
  static const tagline = 'Sabi, yu business operations manager.';
}
