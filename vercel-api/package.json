{
  "name": "sabibom-api",
  "version": "1.0.0",
  "private": true,
  "description": "SabiBom secure Vercel API for Groq and Pinata",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts || true",
    "test": "vitest run",
    "test:watch": "vitest",
    "vercel:dev": "vercel dev"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@vercel/node": "^5.1.8",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
