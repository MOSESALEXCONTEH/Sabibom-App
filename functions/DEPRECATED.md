# Deprecated — use `vercel-api/`

Firebase Cloud Functions for Groq and Pinata are no longer the production backend
because this Firebase project is not on the Blaze plan.

The active API lives in:

`../vercel-api`

Flutter now calls Vercel HTTPS endpoints with Firebase ID tokens.
